// keyword rosetta control shell: csharp / a
// decoy: config reads are safe and no goto word runs outside prose
using b;

static int ProbeGlobals(int env) {
    var region = ConfigurationManager;
    var home = AsyncLocal;
    return env;
}

[Test]
static int ProbeTest(int kit) {
    Assert.True(kit > 0);
    return kit;
}

static int ProbeSafety(int value) {
    var ok = value is int;
    var name = nameof(value);
    return value;
}
