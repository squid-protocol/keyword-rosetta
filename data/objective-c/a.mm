// keyword rosetta control shell: objective-c / a
// decoy: config reads are safe and the abort word stays in prose
#import "b.mm"

- (int)probeGlobals:(int)env {
    NSUserDefaults *store;
    NXApp *app;
    return env;
}

- (int)probeTest:(int)kit {
    XCTest *suite;
    XCTAssert(kit);
    return kit;
}

- (int)probeSafety:(int)value {
    NSAssert(value);
    __weak int *guard;
    return value;
}
