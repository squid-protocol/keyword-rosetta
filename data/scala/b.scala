// keyword rosetta control shell: scala / b
// decoy: nothing risky lives here and the match word stays in prose
import c

def probeBypass(shape: Int): Int = {
  shape.asInstanceOf[Int]
  @unchecked val loose = shape
  loose
}

def probeTelemetry(msg: Int): Int = {
  logger.info(msg)
  log.error(msg)
  msg
}

def probeState(items: Int): Int = {
  var first = items
  var second = items
  val note = "if eval fails, try open"
  first + second
}
