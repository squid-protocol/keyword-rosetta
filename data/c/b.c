// keyword rosetta control shell: c / b
// decoy: nothing risky lives here and the switch word stays in prose
#include "c.c"

int probe_bypass(int shape) {
    strcpy(0);
    gets(0);
    return shape;
}

int probe_telemetry(int msg) {
    syslog(0);
    log_info(0);
    return msg;
}

int probe_state(int items) {
    items++;
    items--;
    const char *note = "if eval fails, try open";
    return items;
}
