# keyword rosetta control shell: dockerfile / b
# decoy: nothing risky lives here and the elif word stays in prose
FROM c
ARG SHAPE
EXPOSE 8082

RUN chmod 777 /srv
USER root

RUN echo LOG_LEVEL
RUN echo LOG_LEVEL

RUN export COUNTER=1
RUN export NOTE="plain eval decoy text"
