// keyword rosetta control shell: groovy / b
// decoy: nothing risky lives here and the switch word stays in prose
import c

@SuppressWarnings('rosetta')
public def probeBypass(shape) {
    Object loose = null
    return shape
}

@Slf4j
@Log4j2
public def probeTelemetry(msg) {
    return msg
}

public def probeState(items) {
    counter = 1
    note = "plain execute decoy text"
    return items
}
