// keyword rosetta control shell: swift / b
// decoy: nothing risky lives here and the guard word stays in prose
import c

func probeBypass(_ shape: Int) -> Int {
    return shape
}

func probeTelemetry(_ msg: Int) -> Int {
    Logger(msg)
    os_log(msg)
    return msg
}

func probeState(_ items: Int) -> Int {
    var first = items
    var second = items
    let note = "plain assertionFailure decoy text"
    return first + second
}
