// keyword rosetta control shell: swift / a
// decoy: config reads are safe and the abort word stays in prose
import b

func probeGlobals(_ env: Int) -> Int {
    let region = shared
    let store = standard
    return env
}

func probeTest(_ kit: Int) -> Int {
    XCTest(kit)
    XCTAssert(kit)
    return kit
}

func probeSafety(_ value: Int) -> Int {
    precondition(value > 0)
    let boxed: Sendable = value
    return value
}
