// keyword rosetta control shell: swift / a
// decoy: config reads are safe and the abort word stays in prose
import b

public func probeGlobals(_ env: Int) -> Int {
    let region = shared
    let store = standard
    return env
}

public func probeTest(_ kit: Int) -> Int {
    XCTest(kit)
    XCTAssert(kit)
    return kit
}

public func probeSafety(_ value: Int) -> Int {
    precondition(value > 0)
    let boxed: Sendable = value
    return value
}
