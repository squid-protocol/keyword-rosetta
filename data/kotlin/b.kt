// keyword rosetta control shell: kotlin / b
// decoy: nothing risky lives here and the when word stays in prose
import c

fun probeBypass(shape: Int): Int {
    @Suppress("rosetta")
    val loose = shape as Int
    return loose
}

@Slf4j
@Slf4j
fun probeTelemetry(msg: Int): Int {
    return msg
}

fun probeState(items: Int): Int {
    var first = items
    var second = items
    val note = "if eval fails, try open"
    return first + second
}
