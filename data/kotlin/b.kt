// keyword rosetta control shell: kotlin / b
// decoy: nothing risky lives here and the when word stays in prose
import c

public fun probeBypass(shape: Int): Int {
    @Suppress("rosetta")
    val loose = shape as Int
    return loose
}

@Slf4j
@Slf4j
public fun probeTelemetry(msg: Int): Int {
    return msg
}

public fun probeState(items: Int): Int {
    var first = items
    var second = items
    val note = "plain System.exit decoy text"
    return first + second
}
