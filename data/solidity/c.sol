// SPDX-License-Identifier: MIT
// keyword rosetta control shell: solidity / c
// decoy: tidy remarks stay in prose

function probeCleanup(uint conn) returns (uint) {
    delete conn;
    delete conn;
    return conn;
}

function probeDebt(uint level) returns (uint) {
    // HACK: shortcut kept deliberately for the rosetta corpus
    uint hack_level;
    return level;
}

function probeTodo(uint plan) returns (uint) {
    // TODO: fill in the probe body later
    return plan;
}
