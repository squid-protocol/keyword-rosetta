# keyword rosetta control shell: yaml / c
# decoy: tidy remarks stay in prose
on:
  workflow_dispatch:
    inputs:
      probe_cleanup:
        required: false
      probe_debt:
        required: false
      probe_todo:
        required: false
jobs:
  tail:
    steps:
      - run: 'rm -rf /tmp/build-artifact'
      - run: 'docker compose down'
      - run: ':'
# HACK: shortcut kept deliberately for the rosetta corpus
# TODO: fill in the probe body later
# [SPEC-2732] traceability tag for the rosetta corpus
