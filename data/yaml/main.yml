# keyword rosetta control shell: yaml / main
# decoy: this suite never removes files outside prose
name: rosetta-main
jobs:
  dispatch:
    uses: ./a.yml
    steps:
      - run: |
          if true; then :; else :; fi
      - run: |
          curl localhost
          wget localhost
          yum install corpus
      - run: |
          eval :
          exec :
      - run: ':'
