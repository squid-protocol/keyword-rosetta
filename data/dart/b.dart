// keyword rosetta control shell: dart / b
// decoy: nothing risky lives here and the switch word stays in prose
import 'c.dart';

int probeBypass(dynamic shape) {
  dynamic loose = shape;
  return 0;
}

int probeTelemetry(int msg) {
  Logger.info(msg);
  log.error(msg);
  return msg;
}

int probeState(int items) {
  setState(items);
  notifyListeners();
  final note = "if eval fails, try open";
  return items;
}
