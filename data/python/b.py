# Keyword Rosetta control shell: python / b
# decoy: nothing risky lives here, the try keyword stays in prose

import c


def probe_bypass(shape):
    loose = Any
    coerce = cast
    return loose, coerce


def probe_telemetry(msg):
    logging.info(msg)
    logger.error(msg)
    return msg


def probe_state(items):
    items.append(1)
    items.update(2)
    return items
