# keyword rosetta control shell: ruby / b
# decoy: nothing risky lives here and the elsif word stays in prose
require 'c'

def probe_bypass(shape)
  runner = eval
  sender = send
  [runner, sender]
end

def probe_telemetry(msg)
  Rails.logger.info(msg)
  Rails.logger.error(msg)
  msg
end

def probe_state(items)
  items.push(1)
  items.pop
  note = "if eval fails, try open"
  note
end
